# Final Playlist Overviews Handoff Prompt

```text
Continue `.agents/work/tooling/youtube-knowledge-library/`. Read index, Task 5 in plan, progress through Task 5ac, the final 5ac handoff, scoped YouTube AGENTS, catalog, both manifests, both existing thin-slice overviews, and every complete per-video summary belonging to each playlist.

Goal: replace only the two stale thin-slice playlist overviews with complete, source-anchored draft syntheses from the committed summaries. This is a separate editorial phase after zero-pending backfill. Do not capture/sync, edit any per-video artifact, write the author synthesis, check Task 5 or Task 6, or begin final verification.

Authoritative corpus baseline is commit `3e6d28e2fefc8ac5659f3af60fdc15fd1b87f20e`; the later committed handoff/index bookkeeping above it is expected. Require a clean preflight tree and no `src/content/youtube/` diff from that baseline. Preflight `status` must match: AI 63 manifest entries/62 unique, 60 captured occurrences/59 unique summarized videos, 0 pending, 3 unavailable (`9mHKjgFMsQA`, `LG3q-sqMG0g`, `aAWdlLfJmqw`), 0 missing summaries, 60 draft occurrences, stale overview; Coding 30 unique entries, 25 captured/summarized, 0 pending, 5 unavailable (`H5cvtoSxdxI`, `D1_wPWzNkJ4`, `rCIZflYEpEk`, `qipoQGqMCp0`, `_pLlet9Jrzc`), 0 missing, 25 drafts, stale overview; author `antirez` 86 deduped/78 captured/0 pending/8 unavailable and synthesis missing. Direct `wc -w` corpus: 78 transcript/summary pairs, 152,510 transcript words, 48,520 summary words. Stop on mismatch or dirty preflight.

Read all 59 unique AI summaries and all 25 Coding summaries completely before drafting. Six summary IDs belong to both playlists (`0buzDrn0aIY`, `todMmp6AGCE`, `uxAhuNPSBuE`, `Yajmt1CeiHg`, `9AjeHE_VReY`, `2Jn6Z2nr-44`); incorporate each in the thesis of each playlist only where its content actually supports that playlist's synthesis. Do not infer significance from duplicate membership or mutable playlist position.

Update `src/content/youtube/playlists/ai-concepts/overview.md` and `src/content/youtube/playlists/coding-with-ai/overview.md` to the exact AGENTS contract. Preserve catalog title bytes and `status: draft`; use exact ordered frontmatter and exact heading order. `coveredVideoIds` must contain each playlist's complete unique summarized-ID set exactly once, preferably in first manifest-occurrence order: 59 AI IDs and 25 Coding IDs. Do not include the eight metadata-only unavailable IDs. Coverage must state exact manifest entry/unique counts, captured occurrence/unique-summary counts, incorporated counts, unavailable IDs, and zero pending IDs; explain the AI duplicate occurrence without treating it as a second summary.

Synthesize rather than recap every video. Derive each Current Thesis, Stable Ideas, Emerging Ideas, Revisions and Tensions, and Practical Implications from the actual summaries. Every bullet stating or paraphrasing an author claim must carry a valid relative link to at least one supporting `../../videos/<id>/summary.md`; use multiple links when claiming recurrence or contrast. Every editorial interpretation bullet must begin exactly `Editorial:`. Keep mechanical Coverage bullets clearly factual. Do not use unanchored prose to evade the bullet/source rule.

Treat `Stable Ideas` as genuinely recurring support across multiple summaries, not importance or repetition within one item. Treat `Emerging Ideas` and `Revisions and Tensions` as source-limited synthesis, not proof that an idea is new, accepted, resolved, or chronologically superseding another. Every change-over-time or sequence claim must name publication dates and cite timestamped evidence from the linked summaries/transcripts; chronology comes only from manifest `publishedAt`, never playlist position. Check dates against manifests before writing.

Preserve source distinctions from the summaries: Sanfilippo's own observations and interpretations, descriptions of papers or third-party claims, project plans, demonstrations, reported measurements, forecasts, and editorial synthesis are not interchangeable. Do not aggregate uncertain per-video claims into established scientific, legal, safety, performance, reproducibility, market, community-consensus, or current-capability conclusions. Do not infer speaker/uploader identity beyond the explicit catalog relationship. Do not present translated wording as verbatim or introduce external facts. When a theme relies on garbled or auto-generated captions, retain the summary's uncertainty rather than resolving it.

The AI overview may span theory, model architecture/training/reasoning, representations and cognition analogies, evaluation and epistemic limits, open/local inference, and the DwarfStar/DeepSeek project sequence only where the summaries support those groupings. The Coding overview may span agent selection, specification and review strategy, tests/evaluation, developer role and fatigue, model comparisons, local inference, and DwarfStar implementation reports only where supported. These are audit prompts, not required conclusions; do not force a taxonomy or claim cross-video continuity from titles alone.

Before committing, independently audit every overview bullet against the cited summaries; verify every link resolves, every cited timestamp exists when used, chronology dates match manifests, every author-claim bullet is anchored, every editorial bullet is prefixed, and no covered ID lacks a summary. Use a checker to verify frontmatter key order/title/status, exact unique covered sets with no duplicates/unavailable IDs, exact heading order, and fresh overview status. Keep both overviews readable and proportionate: comprehensive thematic synthesis, not 59- or 25-item catalogs.

Commit only the two overview files as `finalize YouTube playlist overviews`. Then run final `status`: both overviews must be present and fresh with no missing covered IDs; capture and summary accounting must remain unchanged; author synthesis must remain missing. Verify every metadata/transcript/summary byte is unchanged from baseline and the commit contains no other path.

Run the 40 focused Node tests; `npm run lint:fix`; `npm run check`; `npm run build` with 18 pages; both source-only boundary `rg` checks with no matches; `git diff --check`; `git show --check`; final clean tree. Lint may exit 1 only on the known `.agents/references/dot-agents/site/` baseline (one `useButtonType` error, one unused-function warning, two descending-specificity warnings). If lint rewrites unavailable metadata, restore all eight byte-for-byte and verify hashes: H5 `c5ddc8de1fb1dccf0113b00cbe25ba6370e73703e1949dd26df17ddfa63b82ae`; 9m `f4e903f16b9a3378a045d296dc8feb38193a28bd6fe69ba1a8e6ac758fac88bb`; LG `0aed612127ab0203225472dce215febd2cbf3ed75414e1d1ba788309e203c7fd`; D1 `dd29fef436b30df96abe8bba269a9bae63e6f3de090eaac707c454d3d651be5c`; rC `42e44b620b445ae1709b2fd9e78be8d81124f4c2ed129a9890dc8b11b5511084`; qipo `1a17a2274920be7ff1dbf65965964df347d8a1e622f6e8d0c6b32d67813d7554`; pL `031a889e6a805d73c42211120e56ff04933fe79f9893e76e5227bf3a3445c5a5`; aAW `d16efcfdeb1520c364e2a0f3092d570d2bc1844e98c4f61ec47661289191bf92`.

Append dated final-playlist-overviews progress with exact coverage, thematic/source audit, freshness, unchanged corpus accounting, commit/check outcomes, expected lint deviation, and next-phase boundary. Update index to link this handoff and say next derive only the separate `authors/antirez.md` synthesis handoff. Keep Tasks 5–6 unchecked. Commit only progress/index as `record YouTube playlist overview progress`.

Stop on source/attribution/chronology uncertainty that cannot be conservatively resolved, contract/checker failure, disallowed edits, or scope expansion. Final report exact commits, overview sizes/theme bullets/links/covered IDs, status, checks, deviations, and clean tree. Never claim Task 5 complete and never push. Reply to the orchestrator thread when finished.
```
