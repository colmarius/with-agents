# Task 5d Handoff Prompt

```text
You are continuing the work item at:

.agents/work/tooling/youtube-knowledge-library/

Read first: index.md, plan.md Task 5, progress.md through Task 5c, src/content/youtube/AGENTS.md, catalog.json, both manifests, and existing summaries as contract references.

Goal:
Run the third bounded backfill batch: at most two capture attempts per playlist, then one real source-checked draft summary per successful transcript. This is Task 5d only. Do not update synthesis, complete Task 5, or start Task 6.

Batch decision:
- Run `--limit 2` once for each playlist, maximum four attempts/summaries, no refill.
- Across Tasks 5a–5c, ten attempts produced nine captures and one durable strict-language unavailable, with no throttle/transient/fatal result. Do not enlarge: observed source sizes vary 18-fold and editorial output is not proportional to transcript length.
- Derive Task 5e from this batch's actuals rather than automatically repeating.

Hard boundaries:
- No sync, retry, force, tooling/test/package/catalog/AGENTS/manifest/existing-corpus edits, overview changes, author file, Task 5 checkbox, or Task 6.
- Leave both stale overviews untouched.
- Every success receives a real source-checked summary unless an explicit blocker is recorded. No placeholders.

Preflight:
- Worktree clean; `npm run youtube:library -- help` exits 0.
- Confirm `npm run youtube:library -- status` baseline:
  - ai-concepts: 63/63; 6 captured entries/5 unique; 57 pending; 0 unavailable; 0 missing summaries; overview stale for 2VnxJafIfEI, T5b69_8f5MI, wb7WMD8BtCQ, vIUzIYpmb9Q.
  - coding-with-ai: 30/30; 4 captured; 25 pending; 1 unavailable H5cvtoSxdxI; 0 missing summaries; overview stale for WoaulxVqUUA, ro9ZPPoajJg, WiJ2SaI-BtY.
  - antirez: 86 deduped; 9 captured; 76 pending; 1 unavailable; author file missing.
- Stop before capture on mismatch.

Capture exactly once per playlist, sequentially:
1. `npm run youtube:library -- capture --playlist ai-concepts --limit 2`
2. `npm run youtube:library -- capture --playlist coding-with-ai --limit 2`

- Expected heads: YODJnfuRPAY, 9mHKjgFMsQA; then 1HTtYNaCtcM, IoE3Hi2zpwk. Actual output is authoritative.
- Record every line/exit.
- captured: transcript+metadata. unavailable: metadata only, no summary/refill. ordinary transient: no write, continue other planned command. TooManyRequest/stopped or fatal: stop further capture and summarize prior successes.
- Global dedupe remains authoritative. Shared IDs are 0buzDrn0aIY, 2Jn6Z2nr-44, 9AjeHE_VReY, Yajmt1CeiHg, todMmp6AGCE, uxAhuNPSBuE; none is an expected head. Distinguish entry/unique counts if actual selection reaches one.

Commit capture before editorial work:
- Only new videos/** transcript+metadata (or unavailable metadata-only) may appear.
- `git diff --check`; commit as `capture YouTube backfill batch 3 transcripts`.

For every success:
- Verify requestedLanguage it, actual language it or it-*, kind caption|auto-generated, matching IDs, non-empty timestamped transcript.
- Find manifest entries in both playlists. Use selected-playlist title/publishedAt if shared mutable titles differ; record discrepancy.
- Read full transcript, metadata, and manifest entry.
- Write summary.md exactly per AGENTS.md: ordered provenance, en/draft, adapted first-line disclosure, exact headings/source links, no translated verbatim claims or unsupported/live-data material.
- Every Key Ideas timestamp endpoint must exist in the sibling transcript and support its claim.

Before summary commit:
- Run `npm run lint:fix`; accept only the known reference-site baseline. If it reformats generated JSON, restore the committed generated bytes; never include mechanical changes to capture records or unrelated files.
- `git diff --check`; commit new summaries only as `add YouTube backfill batch 3 summaries`.

Expected status:
- Zero missing summaries among captures; both overviews remain stale with new IDs; author file remains missing.
- Full-success projection: ai 8 captured entries/7 unique, 55 pending; coding 6 captured, 23 pending, 1 unavailable; author 13 captured, 72 pending, 1 unavailable. Record actual outcomes/shared effects.

Allowed files:
- New src/content/youtube/videos/<id>/{transcript.md,metadata.json,summary.md}
- .agents/work/tooling/youtube-knowledge-library/{progress.md,index.md}

Verification:
- `npm run youtube:library -- status`
- `node --test .agents/scripts/youtube-library.test.mjs .agents/scripts/youtube-transcript-core.test.mjs`
- `npm run check`
- `npm run build`
- Both source-only/import `rg` checks find no matches.
- `git diff --check` per commit; final worktree clean.

Work-item update:
- Append Task 5d progress with every outcome/exit, artifact and transcript/summary bytes/words/chunks/anchors, final status, verification, cumulative failure/throttle evidence, deviations, and next action.
- Add handoff-task-5d.md in index and set Next Action to deriving Task 5e from actuals.
- Leave Tasks 5–6 unchecked.
- Commit as `record YouTube backfill batch 3 progress`.

Stop conditions:
- Dirty/baseline mismatch, provenance/language mismatch, unsupported editorial claim, disallowed-file need, throttle, or fatal outcome. Throttle/fatal stops capture but not summaries for prior successes.
- Never broaden capture or update synthesis.

Final report / Task 5e evidence:
- Commits, commands/outcomes, actual status, verification, blockers.
- Per transcript bytes/words/chunks; per summary bytes/words/verified endpoints.
- Compare Task 5b (8,538→1,563 words) and 5c (3,123→1,391), including spread.
- Report pending counts and cumulative evidence across fourteen planned attempts through full Task 5d, then recommend Task 5e size from actuals.
- Do not claim Task 5 complete.
```
