# Antirez Author Synthesis Handoff Prompt

```text
Continue `.agents/work/tooling/youtube-knowledge-library/`. Read index, Task 5 in plan, progress through the final overview audit correction, the final playlist-overviews handoff, scoped YouTube AGENTS, catalog, both current playlist overviews, both manifests, and all 78 unique committed per-video summaries.

Goal: create only the missing draft author synthesis at `src/content/youtube/authors/antirez.md`, then record work-item progress. This is a separate editorial phase after complete backfill and current playlist overviews. Do not capture/sync, edit any existing YouTube artifact, research or add external identity data, check Task 5 or Task 6, or begin final verification.

Authoritative corpus/overview baseline is clean commit `d8a3155` (`record YouTube overview audit correction`); later committed handoff/index bookkeeping above it is expected. Require a clean preflight tree and no `src/content/youtube/` diff from that baseline. Preflight `status` must match: both playlist overviews current; AI 63 entries/60 captured occurrences/0 pending/3 unavailable/0 missing summaries/60 drafts; Coding 30/25/0/5/0/25; author `antirez` 86 deduped IDs/78 captured/0 pending/8 unavailable and synthesis missing. Direct `wc -w` corpus remains 78 transcript-summary pairs, 152,510 transcript words, and 48,520 summary words. Stop on mismatch or dirty preflight.

Catalog identity is authoritative and deliberately minimal: author ID/slug `antirez`, display name `Salvatore Sanfilippo`, related through catalog IDs to the `AI concepts` and `Coding with AI` playlists. No channel URL, personal site, or GitHub profile is configured. In Source Identities, state only this explicit catalog mapping, link the two playlist overviews as `../playlists/ai-concepts/overview.md` and `../playlists/coding-with-ai/overview.md`, and say that no external identity URL is configured. Do not infer identity from transcript `channel`, uploader metadata, titles, speaker references, or external knowledge, and do not modify catalog data.

Read both approved overviews and all 78 unique summaries completely before drafting. Use the overviews as the cross-playlist thematic layer and representative summaries as claim anchors; do not duplicate manifest membership or turn the author file into 78 per-video recaps. Six captured IDs belong to both playlists (`0buzDrn0aIY`, `todMmp6AGCE`, `uxAhuNPSBuE`, `Yajmt1CeiHg`, `9AjeHE_VReY`, `2Jn6Z2nr-44`); count and consider each once in author coverage, and do not infer importance from shared membership.

Create the missing `authors/` directory and `antirez.md` only because the contracted author artifact requires it. Use exact ordered frontmatter: `authorId: antirez`, `status: draft`, then a two-space-indented `coveredVideoIds` block. The covered set must equal all 78 unique summarized IDs across the catalog-related playlists, each exactly once, excluding all eight metadata-only unavailable IDs. `8gg-oJr4dTY` occurs twice within the AI manifest and must still appear only once. Order the set deterministically by catalog relationship playlist order, then first manifest occurrence, deduplicating both that occurrence and cross-playlist shared IDs. Use exact heading order and no substitutes: `## Source Identities`, `## Cross-Playlist Synthesis`, `## Changes Over Time`.

Cross-Playlist Synthesis must compare the two approved playlist syntheses rather than merely concatenate them. Derive a concise author-level account only where the sources support it: for example, how conceptual claims about representations, reasoning, evidence, and model limits relate to engineering claims about agent supervision, behavioral testing, local inference, and DwarfStar work. These examples are audit prompts, not required conclusions. Do not force continuity, consistency, influence, motivation, or a unified worldview from titles, playlist placement, or repeated terminology.

Every bullet stating or paraphrasing an author claim must include at least one valid relative representative-summary link such as `../videos/<id>/summary.md`; use multiple links across both playlists for cross-playlist recurrence or contrast. Every editorial interpretation bullet must begin exactly `Editorial:`. Link the playlist overviews where describing their syntheses, but do not treat an overview link alone as a substitute for representative summary anchors on an author claim. Do not use unanchored prose to evade the claim/source rule.

Changes Over Time must use publication chronology, never playlist order. Every change, persistence, shift, expansion, or sequence bullet must state the relevant ISO publication dates (or unambiguous dates copied from manifests), link representative summaries, and include real sibling-transcript timestamp evidence in `[HH:MM:SS]` or range form. Verify each date against the manifest-selected provenance and every timestamp endpoint against the corresponding transcript. The summarized author corpus runs from `2024-12-21T13:17:00Z` (`vIUzIYpmb9Q`) through `2026-07-13T06:00:31Z` (`XZZ_ddBvELc`), but endpoints alone do not establish a trend. Prefer a few source-supported periods or contrasts over a forced video-by-video timeline.

Preserve source distinctions: Sanfilippo's observations and interpretations, descriptions of papers or third-party reports, project plans, demonstrations, reported measurements, forecasts, and editorial synthesis are not interchangeable. Do not aggregate provisional per-video material into established scientific, legal, safety, performance, reproducibility, market, community-consensus, biographical, psychological, or current-capability conclusions. Do not claim independent reproduction, completed future work, stable project lineage, personal intent, endorsement, or causation unless the cited summaries support that exact statement. Preserve uncertainty from auto-captions and never present English paraphrase as verbatim or add external facts.

Before committing, audit every body bullet against cited summaries and both overview syntheses. Verify exact frontmatter key order/status/author ID, exact 78-ID covered set with no duplicates/unavailable IDs, exact heading order, both overview links, every summary link resolving, every author-claim bullet anchored, every editorial bullet prefixed, and every chronology date/timestamp valid. Keep the synthesis readable and proportionate; frontmatter provides coverage, while the body provides cross-playlist synthesis rather than a catalog.

Commit only `src/content/youtube/authors/antirez.md` as `add YouTube antirez author synthesis`. Then run `status`: both playlist overviews and the author synthesis must be current, with no missing covered IDs; capture/summary/overview accounting must remain unchanged. Verify every pre-existing `src/content/youtube/` byte is unchanged from baseline and the synthesis commit contains no other path.

Run the 40 focused Node tests; `npm run lint:fix`; `npm run check`; `npm run build` with 18 pages; both source-only boundary `rg` checks with no matches; `git diff --check`; `git show --check`; final clean tree. Lint may exit 1 only on the known `.agents/references/dot-agents/site/` baseline (one `useButtonType` error, one unused-function warning, two descending-specificity warnings). If lint rewrites unavailable metadata, restore all eight byte-for-byte and verify hashes: H5 `c5ddc8de1fb1dccf0113b00cbe25ba6370e73703e1949dd26df17ddfa63b82ae`; 9m `f4e903f16b9a3378a045d296dc8feb38193a28bd6fe69ba1a8e6ac758fac88bb`; LG `0aed612127ab0203225472dce215febd2cbf3ed75414e1d1ba788309e203c7fd`; D1 `dd29fef436b30df96abe8bba269a9bae63e6f3de090eaac707c454d3d651be5c`; rC `42e44b620b445ae1709b2fd9e78be8d81124f4c2ed129a9890dc8b11b5511084`; qipo `1a17a2274920be7ff1dbf65965964df347d8a1e622f6e8d0c6b32d67813d7554`; pL `031a889e6a805d73c42211120e56ff04933fe79f9893e76e5227bf3a3445c5a5`; aAW `d16efcfdeb1520c364e2a0f3092d570d2bc1844e98c4f61ec47661289191bf92`.

Append dated author-synthesis progress with exact coverage, source-identity boundary, thematic and chronology audit, artifact size/bullets/links/timestamp counts, current status, unchanged corpus/overview accounting, commit/check outcomes, and expected lint deviation. Update index to link this handoff and say next derive only the dedicated final Task 5 verification/checkoff handoff. Keep Tasks 5–6 unchecked. Commit only progress/index as `record YouTube author synthesis progress`.

Stop on identity/source/attribution/chronology uncertainty that cannot be conservatively resolved, contract/checker failure, disallowed edits, or scope expansion. Final report exact commits, covered-set/accounting, synthesis size and audit counts, checks, deviations, and clean tree. Never claim Task 5 complete and never push. Reply to the orchestrator thread when finished.
```
