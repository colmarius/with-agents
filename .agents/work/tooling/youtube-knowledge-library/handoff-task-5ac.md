# Task 5ac Handoff Prompt

```text
Continue `.agents/work/tooling/youtube-knowledge-library/`. Read index, Task 5 plan, progress through 5ab, 5ab handoff, YouTube AGENTS, catalog/manifests, and established summary patterns.

Goal: final bounded backfill batch only: one `ai-concepts --limit 1` capture command, then one complete source-checked draft if the sole attempt succeeds. Coding has zero pending and must not receive an empty capture command. Reaching zero pending ends backfill only: Task 5 stays in progress pending separate overview, author-synthesis, and final-verification phases; no overview/author synthesis or Task 6 in this batch.

Evidence: 85 attempts through 5ab = 77 captures + 8 durable strict-language unavailable, zero ordinary transient/fatal/throttle/TooManyRequest/stopped/retry. Current direct corpus recount: 151,136 transcript words and 47,376 summary words across 77 captured videos; individual transcripts span 241–5,055 words and summaries 235–1,288. The actual queue contains only one pending ID, so use exactly one attempt with no refill; stop capture on throttle/fatal/stopped.

Forbidden: any Coding capture command; sync/retry/force/repeat/refill; tooling/tests/package/catalog/AGENTS/manifests/existing corpus; overview/author synthesis/placeholders; Task 5 check; Task 6. Allow only the newly selected AI metadata, a successful transcript/summary if captured, then progress/index.

Clean preflight/help exit 0/status must match: AI 63 entries/59 captured occurrences/58 unique/1 pending/3 unavailable (`9mHKjgFMsQA`, `LG3q-sqMG0g`, `aAWdlLfJmqw`)/0 missing/59 drafts; Coding 30/25 captured/0 pending/5 unavailable (`H5cvtoSxdxI`, `D1_wPWzNkJ4`, `rCIZflYEpEk`, `qipoQGqMCp0`, `_pLlet9Jrzc`)/0 missing/25 drafts; author 86 deduped/77 captured/1 pending/8 unavailable, synthesis missing; both overviews stale. Stop on mismatch.

Run exactly once, with no refill regardless of outcome:
`npm run youtube:library -- capture --playlist ai-concepts --limit 1`
Expected sole head: `rqXLtmCPcwk` (AI position 62), selected-manifest title bytes `DwarfStar: un mondo di fork è un buon posto dove vivere`, publication date `2026-07-10T09:05:11Z`. It currently occurs once in AI and zero times in Coding and has no local artifacts. Actual CLI output wins; recheck membership and selected-manifest provenance before editorial work.

Outcome rules: captured = metadata + nonempty chunked transcript. Durable unavailable = typed metadata only with requested and available languages, no transcript/summary/refill. Ordinary transient = no persisted record and remains pending; no refill. Throttle/TooManyRequest/stopped/fatal = stop all further capture immediately. This is one planned attempt, not permission to retry until pending reaches zero.

Commit generated capture artifacts first: verify directory/source/metadata IDs, requested `it`, actual `it` or `it-*`, caption kind, nonempty chunks, durable-unavailable shape, selected-manifest provenance, and title bytes. Stage generated files only, run diff-check, and commit `capture YouTube backfill batch 28 transcripts`.

If successful, read the complete transcript, metadata, and selected manifest entry. Write the exact AGENTS summary contract: ordered frontmatter, manifest title/date bytes, metadata language/kind, `summaryLanguage: en`, `status: draft`, caption-kind-adapted first-line disclosure, exact heading order, canonical watch/transcript links. Anchor every Key Ideas bullet to real chunks and verify every endpoint and claim. The projected DwarfStar/fork discussion may mix a named project, personal motivation, ecosystem or governance views, implementation state, compatibility claims, plans, comparisons, and reported measurements: distinguish what exists or was personally tested from proposals, aspirations, analogy, opinion, third-party work, and independently reproduced evidence. Preserve uncertainty around project identity and relationship to upstreams/forks, repository or release status, authorship/contributors, licensing, compatibility, migration, protocols/APIs/data formats, feature completeness, correctness, security, performance, hardware/configuration/workload, benchmarks, adoption, maintenance, governance, and future delivery. Do not infer endorsement, formal affiliation, legal conclusions, production readiness, superiority, community consensus, successful delivery, or an established technical result from the title or fork framing. Never present English translation/paraphrase as verbatim. No placeholders, unsupported current claims, or synthesis. Commit the successful summary only as `add YouTube backfill batch 28 summary`.

If the sole outcome is durable unavailable or ordinary transient, do not create an empty summary commit. Preserve the capture/generated boundary and then record actual progress/index in the final boundary. If successful, preserve all three ordered capture → summary → progress/index commits.

Lint may fail only on the known `.agents/references/dot-agents/site/` baseline. Restore all eight unavailable records byte-for-byte. Required hashes: H5 `c5ddc8de1fb1dccf0113b00cbe25ba6370e73703e1949dd26df17ddfa63b82ae`; 9m `f4e903f16b9a3378a045d296dc8feb38193a28bd6fe69ba1a8e6ac758fac88bb`; LG `0aed612127ab0203225472dce215febd2cbf3ed75414e1d1ba788309e203c7fd`; D1 `dd29fef436b30df96abe8bba269a9bae63e6f3de090eaac707c454d3d651be5c`; rC `42e44b620b445ae1709b2fd9e78be8d81124f4c2ed129a9890dc8b11b5511084`; qipo `1a17a2274920be7ff1dbf65965964df347d8a1e622f6e8d0c6b32d67813d7554`; pL `031a889e6a805d73c42211120e56ff04933fe79f9893e76e5227bf3a3445c5a5`; aAW `d16efcfdeb1520c364e2a0f3092d570d2bc1844e98c4f61ec47661289191bf92`. Record and preserve any new unavailable hash too.

Full-success projection: AI 60 captured occurrences/59 unique/0 pending/3 unavailable; Coding unchanged at 25 captured/0 pending/5 unavailable; author 78 captured/0 pending/8 unavailable; zero missing summaries. AI overview gains one stale ID; Coding overview gains none. A durable unavailable outcome instead projects AI 59 captured occurrences/58 unique/0 pending/4 unavailable and author 77 captured/0 pending/9 unavailable. An ordinary transient leaves the ID pending. Actual status and shared-membership effects override projections.

Verify final status; 40 focused Node tests; `npm run lint:fix` with only known baseline plus restored unavailable hashes; generated capture-byte drift; `npm run check`; 18-page `npm run build`; both source-only boundary `rg` checks with no matches; `git diff --check`; `git show --check`; clean tree. Append dated 5ac progress with exact outcome, sizes/anchors/provenance if successful, unavailable/transient handling, accounting, checks/commits/deviations, and the actual post-backfill state. Keep Tasks 5–6 unchecked. Update index to link 5ac and, only if pending is zero, say next derive a separate final-playlist-overviews handoff; if transient leaves pending, stop and record that evidence instead. Commit progress/index as `record YouTube backfill batch 28 progress`.

Stop on dirty baseline, language/provenance/claim uncertainty, disallowed edits, throttle/fatal/stopped, or scope expansion. Final report actual outcome and remaining counts; never claim Task 5 complete or push. Reply to the orchestrator thread when finished.
```
