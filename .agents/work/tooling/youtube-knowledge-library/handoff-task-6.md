# Task 6 Final Workflow and Publication-Boundary Handoff Prompt

```text
You are completing the work item at `.agents/work/tooling/youtube-knowledge-library/`. Read first: index; Task 6 and final acceptance in plan; progress through the final Task 5 rerun; Task 1–3 implementation history; scoped YouTube AGENTS; `.agents/scripts/lib/youtube-transcript-core.mjs`; `.agents/scripts/lib/youtube-library-core.mjs`; `.agents/scripts/lib/youtube-library-capture-status.mjs`; both focused test files; `save-youtube-transcript.mjs`; `youtube-library.mjs`; package scripts; catalog/manifests; and the final Task 5 rerun handoff.

Goal: implement the smallest durable fix for the force-recapture title-provenance caveat, then perform the complete Task 6 workflow/publication-boundary acceptance. If and only if the focused change and every Task 6 criterion pass, check Task 6, complete the work item, and commit final bookkeeping. Do not change any YouTube corpus, manifest, metadata, transcript, summary, overview, author file, catalog, package/dependency, Astro/public-resource file, or Task 1–5 checkbox. Do not push.

Current baseline is clean commit `13c34e1` (`complete YouTube knowledge library Task 5`); later committed Task 6 handoff/index bookkeeping above it is expected. Tasks 1–5 are checked, Task 6 is unchecked, `src/content/youtube/` is identical to repaired source baseline `2413bbd`, status is current with no pending/missing summaries, and all editorial artifacts remain draft. Require a clean preflight tree, no source diff from `2413bbd`, exact Task/status state, and no unexpected path/history drift before writing.

Run no live network, sync, capture, retry, refill, force, transcript-fetch/save, or regeneration command. Task 6 validates the real-playlist smoke already committed in Task 5 plus deterministic fixture/injected-boundary tests; mutable upstream state must not be refreshed. Never run a hypothetical force check against the real corpus.

Implement only this tooling correction:
- In `.agents/scripts/lib/youtube-library-capture-status.mjs`, make the selected manifest entry's `manifestTitle`, when nonempty after trimming, the authoritative rendered library-transcript title while preserving the original exact string/code points. Fall back to the existing cleaned fetched-metadata title only when the manifest title is empty after trimming, then to `YouTube video <id>`. Do not change shared `cleanText`, segment/channel normalization, manifest normalization, Markdown rendering, or the public single-video CLI.
- In `.agents/scripts/youtube-library.test.mjs`, update the deterministic capture assertion to expect the manifest title rather than fetched metadata title and assert the divergent shared-video fixture uses the first selected manifest title. Strengthen the existing force-replacement fixture with a generic title containing U+00A0 NBSP while fetched metadata supplies a different ordinary-space title; assert the forced transcript's parsed/rendered title preserves the manifest NBSP exactly and the sibling summary remains byte-identical. Use fixture roots only; add no corpus-specific test data or dependency. The focused suite should remain 40 tests unless a genuinely separate test is necessary.
- Prove in-memory/from fixtures that the three repaired corpus titles would remain manifest-exact under this title-selection rule without writing or force-recapturing corpus files.

Before committing, run the two focused Node test files, a targeted Biome check/fix on the two touched script/test files, `git diff --check`, and inspect the diff. The first implementation commit must contain exactly `.agents/scripts/lib/youtube-library-capture-status.mjs` and `.agents/scripts/youtube-library.test.mjs`; commit it as `preserve YouTube manifest title provenance`. Stop if public transcript code, corpus source, more tooling, package metadata, or unrelated files need modification.

Then validate every Task 6 acceptance item fail-closed:
1. Built-in tests: run all focused tests and explicitly map passing test names/evidence to pure playlist normalization, complete pagination, playlist diffing, path containment, deterministic/idempotent atomic writes, duplicate global video-ID capture, strict exact/regional/unavailable language behavior, transient-no-write/pending behavior, throttle stop, non-destructive/force semantics, status, and manifest-title preservation. Add no test framework.
2. Public single-video compatibility: confirm `.agents/scripts/save-youtube-transcript.mjs` remains byte-identical to Task 1 extraction commit `0770b68`, the shared public rendering/normalization path is untouched by the Task 6 commit, `npm run youtube:transcript -- --help` exits 0 with the established CLI surface, core tests pass, and Task 1's committed real fetch smoke still records compatible JSON/human fields, exit behavior, 86 segments, 11 chunks, canonical path, and no write. Do not repeat the network fetch/save.
3. Real-playlist thin slice: audit immutable Task 5 evidence instead of re-contacting YouTube. Verify current manifest hashes/last-modifying commit and Task 5a's real complete initial sync plus repeat `no changes`/unchanged mtimes; fixture pagination; real duplicate `8gg-oJr4dTY` plus global deduplication; strict Italian captures and eight typed language-unavailable outcomes; zero real ordinary transient/throttle/fatal/retry outcomes; and focused transient/throttle tests. State honestly which behaviors were real-run evidence and which are fixture evidence.
4. Current library/corpus integrity: help/status exit 0; AI `63/60/0/3/0/60`, Coding `30/25/0/5/0/25`, author `86/78/0/8`; both overviews/author current; all 78 transcript and summary titles match manifest provenance; direct totals remain 152,510/48,520 words; source bytes remain identical to `2413bbd`; all eight protected unavailable hashes match.
5. Project/publication boundary: `npm run check`; `npm run build` with exactly 18 pages and no library route; no `src/content/youtube` registration/import in content config/routes/components/layouts; no catalog `source-only` marker in `dist/`; no YouTube source in public resource manifests; and no Task 6 change to corpus/Astro/public content.
6. Quality/scope: run `npm run lint:fix`. It may exit 1 only for the known reference-site baseline (one `useButtonType`, one unused-function warning, two descending-specificity warnings). If it rewrites the eight unavailable metadata records, restore them byte-for-byte and verify the established hashes; do not add a broad Biome-ignore/config change. Run final focused tests, `npm run check`, `npm run build`, both boundary `rg` guards, `git diff --check`, cumulative scope checks, both implementation/final `git show --check`, and clean-tree checks.

Treat all 78 per-video summaries, both playlist overviews, and the author synthesis remaining `draft` as explicit remaining optional manual editorial review, not missing implementation or coverage: status must show zero pending videos, zero missing summaries, current synthesis, and no artifact promoted to `reviewed` by Task 6.

If every criterion passes, change only Task 6's checkbox to `[x]` in plan. Append a dated Task 6 section to progress with: title fix and test proof; criterion-by-criterion real-versus-fixture workflow evidence; public CLI compatibility basis; exact commands/exits/counts/hashes; lint restoration; publication/scope checks; no-network boundary; source/corpus immutability; remaining draft editorial review; and commit IDs. Update index to Status `completed`, keep this handoff linked, and set Next Action to none/work item complete while noting optional manual editorial review. Commit only `plan.md`, `progress.md`, and `index.md` as `complete YouTube knowledge library Task 6`.

Stop without Task 6 checkoff on any test/acceptance failure beyond the known lint baseline, unexpected source/status/hash drift, requirement for live mutable upstream access, public CLI compatibility uncertainty, inability to prove manifest-title preservation, source-only leakage, disallowed edit, or scope expansion. Do not weaken acceptance or repair another issue in this phase. Final report both exact commits, acceptance verdicts, test/status/corpus/publication evidence, real-versus-fixture distinction, lint deviation/restoration, manual draft-review state, all task/status state, clean tree, and no push. Reply to the orchestrator thread when finished.
```
