# Final Task 5 Verification and Checkoff Rerun Handoff Prompt

```text
Continue `.agents/work/tooling/youtube-knowledge-library/`. Read index, Task 5 in plan, complete progress from Task 5a through the title-provenance repair, the original failed final-verification handoff, the repair handoff, scoped YouTube AGENTS, catalog, manifests, current overviews, author synthesis, and relevant Task 5 git history before deciding whether acceptance is met.

Goal: perform a fresh, complete final Task 5 verification from the repaired corpus. If and only if every Task 5 acceptance criterion and check below passes, change Task 5 from `[ ]` to `[x]`, append final Task 5 evidence, and update index. Do not edit `src/content/youtube/**`, tooling, tests, packages, public site/resource data, handoffs, or any Task 1–4 checkbox; keep Task 6 unchecked and do not perform or claim Task 6 completion.

Authoritative repaired source baseline is `2413bbd` (`correct YouTube transcript title provenance`); `97be208` is its progress/index record, and later committed rerun-handoff/index bookkeeping above it is expected. Require a clean preflight tree and no `src/content/youtube/` diff from `2413bbd`. `npm run youtube:library -- --help` and `status` must exit 0. Status must show both overviews and `authors/antirez.md` current; AI 63 entries/60 captured occurrences/0 pending/3 unavailable/0 missing summaries/60 drafts; Coding 30/25/0/5/0/25; author 86 deduped/78 captured/0 pending/8 unavailable. Stop without checkoff on any mismatch.

Do not run live `sync`, `capture`, retry, force, refill, transcript regeneration, or any network command. Task 5a's committed real-sync evidence is authoritative: initial manifest commit `192da2f`, then a repeat real sync reported `no changes`, unchanged mtimes, and SHA-256 values `06cff56a2db7531bcefb9489c65f8feac249ff0c74d2aff47d2196f0e09ab653` (AI) and `ee7f8cc5c3390b1151465b57b7f07362e951f9297a50cdf3e164cb94677dbac4` (Coding). Verify current manifest hashes still equal those values and `192da2f` remains their last modifying commit; audit committed evidence rather than refreshing mutable upstream state.

Verify the thin slice preceded broad backfill: `8a52373` contains only one source-language transcript/metadata pair per playlist (`8gg-oJr4dTY`, `XZZ_ddBvELc`), and `a445f4f` contains their complete English draft summaries plus both initial overviews. Confirm strict-language provenance, source anchors, current successor overviews, and that broad batch 1 starts only afterward at `3a5e075`.

Audit bounded-backfill evidence across Task 5a–5ac and git history. Confirm 86 planned attempts = 78 successful unique captures + 8 durable strict-language unavailable records; 28 post-thin-slice bounded batches; no ordinary transient, fatal, throttle/TooManyRequest, stopped, or retry outcome; no refill/force/sync during backfill. Confirm all capture, summary, additive source-correction, overview, author, title-repair, and progress commits remain reviewably scoped. Acknowledge that no ordinary transient occurred in the real run; verify the focused tests prove transient failures persist nothing/remain pending and throttling stops the queue rather than fabricating a real result.

Re-audit the failed gate and repair rather than assuming the blocker is gone. Confirm the failed verification run from baseline `7460dad` checked off nothing and reported exactly three selected-manifest/transcript title mismatches. Confirm `2413bbd` changes exactly the first `title:` line in `497EK7ZQ2FY`, `n3rdoQnN7Co`, and `XZZ_ddBvELc`: one U+0020 byte (`20`) becomes one U+00A0 code point (`c2 a0`) in each, files grow by one byte to 8,878 / 13,657 / 6,349, current hashes equal respectively `d83b057054b1cc76dac84a3a32cf1ec05582c8d24a2dccb52d31763367cacc93`, `b79255675cd7d6a8f6b5df782a2b9777ba44e915011b71898a55fee33be266a4`, and `5efbae1ce46c253ccf7829805843942d0cc2dd2a4aa8ea6953acc62f0de96f93`, and every byte from `sourceUrl:` through EOF remains identical to `7460dad`. Confirm no other YouTube source changed in the repair and `97be208` records it without a plan checkoff.

Run a corpus-wide read-only contract/provenance checker without creating a committed helper. It must parse quoted title scalars rather than compare escaped raw lines and verify:
- 86 unique manifest video IDs resolve to exactly 78 successful video directories with nonempty metadata/transcript/summary trios and 8 metadata-only unavailable directories, with no partial or orphan directory.
- AI has 63 entries/62 unique IDs and the single duplicate `8gg-oJr4dTY`; Coding has 30 unique entries; six captured IDs are shared; author union is 86 unique.
- Every successful metadata record has matching directory/video ID, `requestedLanguage: it`, actual `it` or `it-*`, and `kind` exactly `caption` or `auto-generated`; every unavailable record has the established typed strict-language metadata-only shape and no transcript/summary.
- Every transcript is nonempty with timestamp chunks, matching directory/video/source URL, language/kind, and a decoded title byte-exactly equal to selected manifest provenance. All 78 transcript titles and 78 summary titles must match; every shared ID must have non-conflicting manifest title/date provenance; remaining title mismatch count must be zero.
- Every summary has exact ordered frontmatter, matching ID/canonical URL/manifest title/date/metadata language-kind, `summaryLanguage: en`, `status: draft`, caption-kind-adapted first-line translation/paraphrase disclosure, exact heading order, canonical video/transcript links, an existing sibling-transcript anchor on every Key Ideas bullet, and every timestamp endpoint present. No placeholder counts as complete.
- Direct `wc -w` totals in this environment's locale, as recorded by the repair, remain 152,510 transcript words and 48,520 summary words across 78 pairs. All eight protected unavailable hashes remain exact.

Run synthesis-wide read-only checks. Each playlist overview must have exact contract/title/status/headings, the exact complete unique summarized-ID set in deterministic first-manifest-occurrence order, no duplicate or unavailable covered ID, all summary links resolving, and current status. Expected coverage is 59 unique AI IDs with 103 links after the audit correction and 25 Coding IDs with 77 links. The author synthesis must have exact `authorId: antirez`/draft/frontmatter/headings, exact 78-ID covered set in catalog relationship/first-occurrence order, both overview links, 60 resolving summary links, 12 `Editorial:` bullets, 5 chronology bullets, and 13 manifest-date/summary/timestamp pairs with all 26 endpoints present. Verify only catalog-configured identity is stated. Re-audit the four additive source-attribution corrections (`6703244`, `cf6c8dc`, `fcdf492`, `7a9bdc7`) and the title correction (`2413bbd`) with their progress trail; do not rewrite history or promote editorial status.

Record one non-blocking operational caveat, but do not widen this gate: current transcript `cleanText` whitespace normalization would convert manifest NBSP to ordinary space on a future explicit `--force` recapture. Default capture is non-destructive, no force was used in Task 5, and the repaired committed corpus is authoritative. Carry the caveat into the Task 6 handoff for an explicit workflow/tooling decision; do not edit tooling during Task 5 verification and do not treat the dormant force-only behavior as a fabricated current corpus mismatch.

Verify Task 5 source-only/reviewable scope from post-Task-4 baseline `5f0b210`: changes may be only committed YouTube library source and work-item artifacts/handoffs. There must be no Task 5 change to tooling/tests/package, `src/content.config.ts`, routes/components/layouts, `src/data/resources`, public resource manifests, or unrelated content. Confirm the source-only tree is not registered/imported and no library route or public-resource entry was introduced.

Run the 40 focused Node tests; `npm run lint:fix`; `npm run check`; `npm run build` with exactly 18 pages; `rg -n "src/content/youtube" src/content.config.ts src/pages src/components src/layouts` with no matches; `rg -n "source-only" dist/` with no matches; `git diff --check`; `git diff --check 5f0b210..HEAD`; relevant `git show --check`; and final clean-tree checks. Lint may exit 1 only on the known `.agents/references/dot-agents/site/` baseline (one `useButtonType` error, one unused-function warning, two descending-specificity warnings). If lint rewrites unavailable metadata, restore all eight byte-for-byte and verify hashes: H5 `c5ddc8de1fb1dccf0113b00cbe25ba6370e73703e1949dd26df17ddfa63b82ae`; 9m `f4e903f16b9a3378a045d296dc8feb38193a28bd6fe69ba1a8e6ac758fac88bb`; LG `0aed612127ab0203225472dce215febd2cbf3ed75414e1d1ba788309e203c7fd`; D1 `dd29fef436b30df96abe8bba269a9bae63e6f3de090eaac707c454d3d651be5c`; rC `42e44b620b445ae1709b2fd9e78be8d81124f4c2ed129a9890dc8b11b5511084`; qipo `1a17a2274920be7ff1dbf65965964df347d8a1e622f6e8d0c6b32d67813d7554`; pL `031a889e6a805d73c42211120e56ff04933fe79f9893e76e5227bf3a3445c5a5`; aAW `d16efcfdeb1520c364e2a0f3092d570d2bc1844e98c4f61ec47661289191bf92`.

If every acceptance item passes, change only Task 5's checkbox to `[x]` in `plan.md`. Append a dated final Task 5 verification-rerun section to progress with criterion-by-criterion evidence, checker counts, immutable-sync and thin-slice evidence, batch/accounting evidence, failed-gate/repair proof, synthesis/source-fidelity trail, all commands/exits, known lint deviation, force-recapture caveat, exact scope, and explicit Task 6 exclusion. Keep this rerun handoff linked in index, retain Status `in-progress`, and set Next Action to derive only a dedicated Task 6 verification/final-review handoff that explicitly assesses the force-recapture normalization caveat. Commit only `plan.md`, `progress.md`, and `index.md` as `complete YouTube knowledge library Task 5`.

If any criterion fails, do not check Task 5, do not weaken acceptance, do not edit source, and do not start another fix in this verification scope; report the exact blocker and leave the tree clean. Final report criterion verdicts, exact commit if checked off, corpus/title/synthesis/accounting results, repair proof, commands, deviations/caveat, Task 6 unchecked, clean tree, and no push. Reply to the orchestrator thread when finished.
```
