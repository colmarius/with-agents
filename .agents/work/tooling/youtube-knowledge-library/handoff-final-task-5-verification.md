# Final Task 5 Verification and Checkoff Handoff Prompt

```text
Continue `.agents/work/tooling/youtube-knowledge-library/`. Read index, Task 5 in plan, complete progress from Task 5a through the author audit correction, the Task 5a/final-overview/author handoffs, scoped YouTube AGENTS, catalog, manifests, current overviews, and author synthesis. Inspect relevant Task 5 commit history before deciding whether acceptance is met.

Goal: perform the dedicated final Task 5 verification only. If and only if every Task 5 acceptance criterion and check below passes, change Task 5 from `[ ]` to `[x]`, append final Task 5 verification evidence, and update index. Do not edit `src/content/youtube/**`, tooling, tests, packages, public site/resource data, or any Task 1–4 checkbox; keep Task 6 unchecked and do not perform or claim Task 6 completion.

Authoritative completed-editorial baseline is clean commit `0479c10` (`record YouTube author audit correction`); the later committed handoff/index bookkeeping above it is expected. Require a clean preflight tree and no `src/content/youtube/` diff from that baseline. `npm run youtube:library -- --help` and `status` must exit 0. Status must show both overviews and `authors/antirez.md` current; AI 63 entries/60 captured occurrences/0 pending/3 unavailable/0 missing summaries/60 drafts; Coding 30/25/0/5/0/25; author 86 deduped/78 captured/0 pending/8 unavailable. Stop without checkoff on any mismatch.

Do not run live `sync`, `capture`, retry, force, refill, or any network command. Task 5a's committed real-sync evidence is authoritative: initial manifest commit `192da2f`, then a repeat real sync reported `no changes`, unchanged mtimes, and SHA-256 values `06cff56a2db7531bcefb9489c65f8feac249ff0c74d2aff47d2196f0e09ab653` (AI) and `ee7f8cc5c3390b1151465b57b7f07362e951f9297a50cdf3e164cb94677dbac4` (Coding). Verify current manifest hashes still equal those values and `192da2f` remains their last modifying commit; audit the Task 5a progress and commit boundaries rather than refreshing mutable upstream state.

Verify the thin slice preceded broad backfill: `8a52373` contains only one source-language transcript/metadata pair per playlist (`8gg-oJr4dTY`, `XZZ_ddBvELc`), and `a445f4f` contains their complete English draft summaries plus both initial overviews. Confirm the recorded strict-language provenance, source anchors, current successor overviews, and that broad batch 1 starts only afterward at `3a5e075`.

Audit bounded-backfill evidence across Task 5a–5ac and git history. Confirm 86 planned attempts = 78 successful unique captures + 8 durable strict-language unavailable records; 28 post-thin-slice bounded batches; no ordinary transient, fatal, throttle/TooManyRequest, stopped, or retry outcome; no refill/force/sync during backfill. Confirm capture, summary, correction, overview, author, and progress commits remain reviewably scoped. Acknowledge that no ordinary transient occurred in the real run; verify the focused tests still prove transient failures persist nothing/remain pending and throttling stops the queue rather than fabricating a real transient result.

Run a corpus-wide read-only contract/provenance checker without creating a committed helper. It must verify:
- 86 unique manifest video IDs resolve to exactly 78 successful video directories with nonempty metadata/transcript/summary trios and 8 metadata-only unavailable directories, with no partial or orphan directory.
- AI has 63 entries/62 unique IDs and the single duplicate `8gg-oJr4dTY`; Coding has 30 unique entries; six captured IDs are shared across playlists; author union is 86 unique.
- Every successful metadata record has matching directory/video ID, `requestedLanguage: it`, actual `it` or `it-*`, and `kind` exactly `caption` or `auto-generated`; every unavailable record has the established typed strict-language metadata-only shape and no transcript/summary.
- Every transcript is nonempty with timestamp chunks, matching directory/video/source URL, language/kind, and byte-exact title from its manifest provenance. Every shared ID has non-conflicting selected-manifest title/date provenance.
- Every summary has exact ordered frontmatter, matching ID/canonical URL/manifest title/date/metadata language-kind, `summaryLanguage: en`, `status: draft`, caption-kind-adapted first-line translation/paraphrase disclosure, exact heading order, canonical video/transcript links, an existing sibling-transcript anchor on every Key Ideas bullet, and every timestamp endpoint present. No placeholder counts as complete.
- Direct `wc -w` totals remain 152,510 transcript words and 48,520 summary words across 78 pairs. All eight protected unavailable hashes remain exact.

Run synthesis-wide read-only checks. Each playlist overview must have exact contract/title/status/headings, the exact complete unique summarized-ID set in deterministic first-manifest-occurrence order, no duplicate or unavailable covered ID, all summary links resolving, and current status. Expected coverage is 59 unique AI IDs with 103 links after the audit correction and 25 Coding IDs with 77 links. The author synthesis must have exact `authorId: antirez`/draft/frontmatter/headings, exact 78-ID covered set in catalog relationship/first-occurrence order, both overview links, 60 resolving summary links, 12 `Editorial:` bullets, 5 chronology bullets, and 13 manifest-date/summary/timestamp pairs with all 26 endpoints present. Verify only catalog-configured identity is stated. Re-audit the four additive source-attribution corrections (`6703244`, `cf6c8dc`, `fcdf492`, `7a9bdc7`) and their progress records as part of the final source-fidelity trail; do not rewrite history or change editorial status to reviewed.

Verify Task 5 source-only/reviewable scope from the post-Task-4 baseline `5f0b210`: changes may be only the committed YouTube library source and work-item artifacts/handoffs. There must be no Task 5 change to tooling/tests/package, `src/content.config.ts`, routes/components/layouts, `src/data/resources`, public resource manifests, or unrelated content. Confirm the source-only tree is not registered/imported and no library route or public-resource entry was introduced.

Run the 40 focused Node tests; `npm run lint:fix`; `npm run check`; `npm run build` with exactly 18 pages; `rg -n "src/content/youtube" src/content.config.ts src/pages src/components src/layouts` with no matches; `rg -n "source-only" dist/` with no matches; `git diff --check`; relevant `git show --check`; and final clean-tree checks. Lint may exit 1 only on the known `.agents/references/dot-agents/site/` baseline (one `useButtonType` error, one unused-function warning, two descending-specificity warnings). If lint rewrites unavailable metadata, restore all eight byte-for-byte and verify hashes: H5 `c5ddc8de1fb1dccf0113b00cbe25ba6370e73703e1949dd26df17ddfa63b82ae`; 9m `f4e903f16b9a3378a045d296dc8feb38193a28bd6fe69ba1a8e6ac758fac88bb`; LG `0aed612127ab0203225472dce215febd2cbf3ed75414e1d1ba788309e203c7fd`; D1 `dd29fef436b30df96abe8bba269a9bae63e6f3de090eaac707c454d3d651be5c`; rC `42e44b620b445ae1709b2fd9e78be8d81124f4c2ed129a9890dc8b11b5511084`; qipo `1a17a2274920be7ff1dbf65965964df347d8a1e622f6e8d0c6b32d67813d7554`; pL `031a889e6a805d73c42211120e56ff04933fe79f9893e76e5227bf3a3445c5a5`; aAW `d16efcfdeb1520c364e2a0f3092d570d2bc1844e98c4f61ec47661289191bf92`.

If every acceptance item passes, change only Task 5's checkbox to `[x]` in `plan.md`. Append a dated final Task 5 verification section to progress with criterion-by-criterion evidence, checker counts, immutable-sync evidence, batch/accounting evidence, synthesis/source-fidelity audit trail, all commands/exits, known lint deviation, exact scope, and explicit Task 6 exclusion. Update index to link this handoff, keep Status `in-progress`, and set Next Action to derive only the dedicated Task 6 verification/final-review handoff. Commit only `plan.md`, `progress.md`, and `index.md` as `complete YouTube knowledge library Task 5`.

If any criterion fails, do not check Task 5, do not weaken the acceptance language, and do not start a fix outside this verification scope; record/report the exact blocker and leave the tree clean. Final report criterion verdicts, exact commit if checked off, accounting/checker results, commands, deviations, Task 6 unchecked, and no push. Reply to the orchestrator thread when finished.
```
