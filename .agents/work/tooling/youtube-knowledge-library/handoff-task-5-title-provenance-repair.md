# Task 5 Transcript Title Provenance Repair Handoff Prompt

```text
Continue `.agents/work/tooling/youtube-knowledge-library/`. Read index, Task 5 in plan, progress through the author audit correction, the failed final Task 5 verification handoff, scoped YouTube AGENTS, both manifests, and the three affected transcript/summary/metadata sets.

Goal: repair only the three transcript frontmatter title-byte mismatches found by the failed Task 5 acceptance gate. Replace one ordinary U+0020 space with the selected manifest's U+00A0 NBSP in each affected `title:` scalar. Do not change transcript bodies, any other frontmatter field, metadata, summaries, manifests, overviews, author synthesis, tooling, tests, packages, plan checkboxes, public-site data, or unrelated files. Task 5 and Task 6 remain unchecked; this repair does not itself satisfy or rerun acceptance.

Authoritative source baseline is clean commit `7460dad` (`add YouTube Task 5 verification handoff`); the later committed repair-handoff/index bookkeeping above `7460dad` is expected, but `src/content/youtube/` must have no diff from it. Require a clean preflight tree and exact current status: both playlist overviews and the author synthesis current; AI 63 entries/60 captured occurrences/0 pending/3 unavailable/0 missing; Coding 30/25/0/5/0; author 86 deduped/78 captured/0 pending/8 unavailable. Run no network, sync, capture, retry, force, refill, or transcript regeneration command.

Before writing, run a robust in-memory checker that parses transcript and summary `title:` values as JSON-compatible quoted scalars rather than comparing escaped raw Markdown lines. It must prove exactly three transcript-title mismatches and no others across all 78 captured videos:
- `497EK7ZQ2FY`: selected AI manifest `Ciò che importa di più del "prediction is / is-not reasoning": pensare agli LLM in altro modo`; transcript currently has U+0020 between `più` and `del`; summary already preserves manifest U+00A0.
- `n3rdoQnN7Co`: selected AI manifest `Kimi K2.5 (bonus point: Open Weights è Open Source?)`; transcript currently has U+0020 between `è` and `Open`; summary already preserves manifest U+00A0.
- `XZZ_ddBvELc`: selected Coding manifest `Controllare le idee è più importante di guardare il codice`; transcript currently has U+0020 between `più` and `importante`; summary already preserves manifest U+00A0.
Stop if the mismatch set, Unicode code points, manifest provenance, or already-correct summary bytes differ.

Edit only the first `title:` line in:
- `src/content/youtube/videos/497EK7ZQ2FY/transcript.md`
- `src/content/youtube/videos/n3rdoQnN7Co/transcript.md`
- `src/content/youtube/videos/XZZ_ddBvELc/transcript.md`
Preserve the existing YAML/JSON escaping for embedded quotation marks. Each file must replace exactly one single-byte U+0020 with the two-byte UTF-8 U+00A0 sequence and therefore grow by exactly one byte. Before-repair transcript sizes/hashes are 8,877 bytes / `52641b9650c6c8ac166abfccd7fc0f941ab890a592df42875307ae966e7e4916` (497), 13,656 / `0c04a0f96508507580207b557baa4fa57404f4e9679c17bee55a344a02d0003a` (n3), and 6,348 / `4f6a8ad6d4ad509c77a473df136db6b2da422bcb60d3439bf6d4caae05f495e8` (XZZ); expected after-repair sizes are respectively 8,878, 13,657, and 6,349 bytes. Verify the only per-file byte delta is that exact title substitution and all bytes from `sourceUrl:` through EOF remain identical to `7460dad`.

After writing, rerun the corpus-wide title checker: all 78 parsed transcript titles and all 78 parsed summary titles must byte-match their selected manifest titles; shared IDs must have non-conflicting manifest title/date provenance; mismatch count must be zero. Re-run complete summary contract/anchor checks for the three affected video sets and confirm metadata, summaries, timestamps, transcript bodies, title escaping, source URLs, IDs, language/kind, and manifest dates remain unchanged/correct. Recount corpus words and record actual totals; do not assume whether `wc -w` treats NBSP as a separator.

Run `git diff --word-diff=porcelain` plus a byte/code-point inspection and `git diff --check`. The staged source commit must contain exactly the three one-line substitutions above and no other path. Commit only those transcripts as `correct YouTube transcript title provenance`.

Then run `npm run youtube:library -- status`; the 40 focused Node tests; `npm run lint:fix`; `npm run check`; `npm run build` with 18 pages; both source-only boundary `rg` checks with no matches; `git show --check`; and final clean-tree checks. Lint may exit 1 only on the known `.agents/references/dot-agents/site/` baseline. If it rewrites unavailable metadata, restore all eight byte-for-byte and verify these hashes: H5 `c5ddc8de1fb1dccf0113b00cbe25ba6370e73703e1949dd26df17ddfa63b82ae`; 9m `f4e903f16b9a3378a045d296dc8feb38193a28bd6fe69ba1a8e6ac758fac88bb`; LG `0aed612127ab0203225472dce215febd2cbf3ed75414e1d1ba788309e203c7fd`; D1 `dd29fef436b30df96abe8bba269a9bae63e6f3de090eaac707c454d3d651be5c`; rC `42e44b620b445ae1709b2fd9e78be8d81124f4c2ed129a9890dc8b11b5511084`; qipo `1a17a2274920be7ff1dbf65965964df347d8a1e622f6e8d0c6b32d67813d7554`; pL `031a889e6a805d73c42211120e56ff04933fe79f9893e76e5227bf3a3445c5a5`; aAW `d16efcfdeb1520c364e2a0f3092d570d2bc1844e98c4f61ec47661289191bf92`. Verify no file other than the three repaired transcripts differs in `src/content/youtube/` from `7460dad`.

Append a dated repair entry to progress that records the failed-gate blocker, exact Unicode/code-point diagnosis, before/after sizes and hashes, zero remaining mismatches, unchanged body/frontmatter/source artifacts, actual word totals/status, commit/check outcomes, and the fact that Task 5 still requires a fresh dedicated acceptance rerun. Keep this handoff linked in index and set Next Action to derive only a new final Task 5 verification/checkoff rerun handoff from the repaired baseline. Keep Tasks 5–6 unchecked. Commit only progress/index as `record YouTube title provenance repair`.

Stop on any additional mismatch, inability to prove the one-byte substitutions, unexpected corpus/status drift, disallowed edit, or check failure beyond the known lint baseline. Final report exact commits, Unicode deltas, before/after hashes/sizes, all-title checker, corpus totals, checks/deviations, both tasks unchecked, clean tree, and no push. Reply to the orchestrator thread when finished.
```
