---
title: "Pi Coding Agent - Deep Dive"
summarySlug: "coding-with-agents/pi-coding-agent-deep-dive"
sourceUrl: "https://www.youtube.com/watch?v=5kLL0xUC28Q"
videoId: "5kLL0xUC28Q"
capturedAt: "2026-08-23T19:33:08.365Z"
channel: "0xSero"
language: "en"
kind: "auto-generated"
durationSeconds: 1799
---

## Transcript

[00:00:00] So, we're going to be talking about Pi. Pi is a harness. I will try to keep up with what you guys are doing in the chat. Hello. Hello. Joining unmuted. Uh if you have questions, leave them in the hangout chat. Uh if you have notes or anything that you might want to explore. Uh you're also free to unmute at any point. Uh and I encourage you to do so. We're going to talk a bit about agent harnesses. So, uh harness agent harness. Let's see. All right, we'll just talk a bit about PI. So PI is an agent harness. It is a codebase that is about 50 to 70,000 lines of code. I saw somebody unmuted. I'll quiet down. F3 DZ. Okay, you're you're done. So PI is an agent harness. It runs in the terminal. We're going to launch a uh ghosty terminal so that we can keep up with it.

[00:00:57] and do PI update. But there are apps that let you connect uh connect Pi. So, I'm not really sure I remember which ones, but you'll definitely find a few here and there. Let's change to uh Ox. Um I'm not really sure where that is. We'll try GM 5.2 just so that we can follow along. So, this is a terminal. Uh ghosty is a terminal. It can do multiplexing. So you can do something like control or command D to open another terminal on its side. Uh command shift W. Okay, I don't need command shift W. Command shift D. Okay, that just keeps uh keeps making new sessions. But each one is a um JavaScript process that's running on your machine. It provides three layers of modules. So modules in this case, imagine them as like components of the system. Uh there are three of them and they're layered.

[00:01:53] If we go to the codebase, so you see this is the PI codebase. Uh there's packages. It's a monor repo. So that means there's multiple repositories in this one uh GitHub repo. Once we get in, we see all of these are different um all of these are different packages that could be used on their own. The base of all of them is AI. So AI does all the translation between different providers. If you look at Anthropic, uh, Anthropic usually serves their models uh, with v1/ messages. So, it's their anthropic API. Uh, it's different than the OpenAI API or the responses API. So, if you were using claude and then you tried to switch to GBT, you would uh, have an issue because these two models speak different languages basically.

[00:02:47] So this is the basis of the entire repo. It's the heart and uh I think it's like very easy to understand. It's really clean. There's not much code and you can pretty much get a sense of what's going on by just looking at the shape of things. Uh this is a lot of documentation but we can just jump in. Let's see session resources. You have index. Index is usually where everything is imported and then exported out. So imagine like this is this just has all the wiring. This is the wiring of the app. And by following the wiring, you can see essentially what uh like what imports what. You can see all of them. Uh this pretty much covers like 95% of the AI market in terms of like the inference providers. So there's Cerebras. You can see here Cerebrris is being imported and exported back out. There's the Cerebrus module models. Um they're importing it from somewhere else. Uh but this speaks the Cerebrris provider language. So you can use Cerebrris in this application. Uh you have the Deepseek one and they're all basically the same. their templates uh that are being filled. Each template is using this create provider function.

[00:03:49] If this is a little too complicated for you guys or you feel like it is not relevant, uh if you have any questions, please feel free to leave them in the uh hangout. So provider is this is what everything is like pulling from. Uh this is the base file that providers that is the shape of the provider. It has all the functionality in here. These are all the interfaces, the types. So when we say models, we know that models have get providers. So a model can have multiple providers. Um or it could fetch like a specific provider using an ID. Uh check off, get off, login, log out. So it's implementing all of these functionalities that when we when we do a login, this is what we're hitting. We're hitting the code that's in here. And login is a function. Somebody unmuted. All right, we'll continue. So, different providers like Claude platform is the anthropic provider. They speak different languages and they have different

[00:04:51] subscriptions. They have different authentication methods. All of that is being handled within this package. All that is being handled here. So you don't have to if you're building an AI app like you are talking to Claude and you're trying to build some app that has some form of AI functionality. These are all problems that you will eventually run into. So it's a good place to get information so that you can apply it in your own products in your own projects or you even just pull in PI and use PI as is. Um so we we talked about providers. providers are like claude or openai or cerebrus your own personal provider as in you bring uh local like llama CPP or whatever into pi so they have a limited amount of uh VRM that they can give and VRM stores three main things you know obviously there's other stuff happening on the GPU but these are the three main things that are stored on the GPU or like what what what is taking up space there's the model weights the model weights are all the numbers the floatingoint numbers in a certain format that the LLM is. So an LLM is simply we can look at config.json to get the the

[00:05:57] point. An an LLM is like a configuration. This is a configuration associated with a bunch of floatingoint numbers. So this config tells you what the model is. Its architecture is when etc etc. So if you look at the inference engine VLM, Llama, CPP, SGLANG. Um so the provider what they're doing is they have an architecture. uh that architecture has code that does math on that architecture. So uh that that's what what is running on the GPUs uh and it is linked to whatever amount of gigabytes of weights in when you see the number 27B that means 27 billion parameters each parameter if it's in 16 bits which is what most um models are published as if it's 16 bits then it would be two bytes per parameter which means 27 billion parameters is going to be uh 54 billion 54 was it? Yeah, 54 54 billion uh 54 billion bytes which is 54 gigabytes.

[00:06:59] You can see like the model like basically okay this is the performance this is the benchmarking but this is all the code for the inference for the different providers. We're going to go back into pi. The agent loop is a for loop. So if you've done programming, you know a for loop is just just keep doing something until some requirement is met and you break out of the loop. So the for loop in this scenario is a loop just like we know and the exit condition is the model outputting the I'm done token. There's a special token called a finish token. So once it outputs finish that means the turn is done and the model output has been provided and it is waiting for the next prompt is waiting for the next thing to do. So the way that the the loop happens is there's like an outer orchestrator script that is doing the looping that is forcing the model to look at what it did and continue doing what it needs to do. And then there's what's going on inside the model. So the model is what decides when things are done when it's when it's done and it needs to stop. That's how it outputs a token. And usually that's based on its training and the trajectory that it's

[00:08:02] going on. So what I mean by trajectory is when you give it a task like edit this video for me or write this code for me there is a statistical probability or set of statistical probabilities where it's going to do some stuff to try to fulfill the request that you've given it and as it's doing that stuff eventually it feels like it is done so it's like it's run its course and it says I'm finished and that tells the outer loop which is pi agent that the turn is over and we can stop. The agent handles a few things. So, uh it handles this this flow. Agent start. So, we've sent the prompt. Turn start. The model has processed the prompt. Message start, it starts writing or like it's reading your prompt. Message end. Okay. Um message start for the model. The model starts responding. Uh it emits a stream. So, what we see also is a stream. We're not getting things in chunks. We're getting things in streams. That's why you see like it's filling bit by bit the sentence over and over again.

[00:09:03] So that's the streaming that we need to that we need to handle. We need to take that stream which has an API structure and we need to turn it into uh a format. There you go. Uh so this loop this agent loop it's doing things as the model is emitting data. Some example of data is it emits a tool call. A tool call is something like uh let's look at an open AI tool call API. So imagine this. This is like a tool. This is the description of a tool. Uh it's get inventory is the name of the tool. You have a description parameters. So that's the stuff that you give in the code when you say get inventory. Like maybe you are getting inventory for a specific item that you're selling. Which models work for free in open router? Try Jail in 5.2 for free, but it throws an error. Find what works.

[00:10:04] There's a free API. Um, and yeah, you can basically filter for free models on Open Router. Let's go to Open Router. Uh, let's go to models. And where's prices? I can't find the prices. Okay, there you go. We want free. So, I would recommend this one, but there's other ones as well. Let's go back. Uh, so if you guys want to play along and you don't have like a subscription or you're out of your sub, you can always use the OpenAI free API. Uh, so the Open Router free API and it'll just give you the best model for the task that is free. Um, and it's always free. There's always like models in the free tier. Obviously, they're dumber, but they can still get the job. Um, okay. So, we're going to go back to the tool. So, tools are the last big part of of this. Uh, let me think how I should explain this. Yeah. So, you have the model that's looping and it can take

[00:11:07] text in and push text out, but then you need it to do stuff otherwise like, yeah, it's just it's just yapping. So you need to give it access to tools and hi thought we should give it access to four tools like read uh so I think it's um bash read file write file and edit file and that's it so the model can do four things and then everything else is a derivative of those four things >> I have a question >> go ahead um what's when you're adding new tools is it something that you would just essentially ask the LLM to add for you to add them in manually like what's the process to adding new tools >> okay So if you if we're talking about like writing and creating your own tools, you have this API format, right? That says this is like what the tool this is what the tool takes in and what it t and what it spits out. Here's who's allowed to call it. Here's type of the tool. Uh the name of the tool, the description. So this is a schema of what your tool takes in and out. Now this would be associated with a function, some code that does something when get inventory is called and all that. So the

[00:12:11] thing is the models are great at writing code. So they can write tools and they can just throw them out like you don't need to have them forever carry their weight. Um the model can write some code like call that code. Uh and yeah it does what it needs to do. So pi is giving you the bash tool and bash is so bash tool pi. So bash is like just this patch alter like tool that can do pretty much anything that you can do on a computer in some way or form. Uh it's it's not perfect like it's it's it's very general. So it's like very it's a very loose tool. Uh it's hard to collect information and data on like how it's being used and all that. Uh but it it it it's like a hammer. You you kind of need it. Uh so it uses bash to do everything and then it creates files which files could be code in themselves and then it uses bash to call those files. So pi again it handles the loop which is until the model is satisfied keep going and keep outputting tokens. The model decides when it's done outputting tokens and it says finish. Uh that means that the loop is is cut and it's ended. Um during the loop, it has access to tools.

[00:13:14] You define the tools that it has access to or you just use a harness that's already pre-made like pi. And pi tells it you can create files, edit files, read files, call bash. That's pretty much all that you need. And you have an agent. Uh let's see. So it's telling me, okay, so there's five. Create goal. Okay, I gave it the create goal tool. So I gave it a custom tool via the extension. Um yeah, plugin, I think. I don't know. I don't know how you get to it. maybe in settings. Uh but the tool is called PI goal and it's just goal from codeex but in pi. So you can do goal and you can say like I don't know something like find all of the sessions that I have had with codeex over the last two days and summarize the what whatever I did wherever I got stuck uh what's still pending and what I need to do next. So I give it a goal and then it just sits there and works until the goal is complete. uh it just makes the model run much longer is essentially what's going on. But this is using a local model. So we go here. This is GLM 5.2 that I'm running locally, but you can also run

[00:14:16] Quen 3.8 27D. Probably most of you can run this model. Um it won't be at like its smartest, but it'll probably run for most of you in some way like three bits. Um but I'm using local models right now to do the the coding and it's just like finding all these sessions. Uh so hopefully that explains the tools part. So you have an agent loops. There is a cache. The KV cache is the history of the conversation. If you fit the history of the conversation on VRAM, so video memory, which is GPUs or let's say mixed memory cards or DGX sparks, if you hit uh you you can store the KV cache on memory and it'll be very fast to process tokens to do prefill. Prefill is when it's reading your prompt. when you send it a message, there's like latency and that latency is because the model is moving the bits onto the onto the GPU and doing all this math to derive all of the tables that it needs for the matrix multiplications. So if there is no cache of that existing already on the the card, it's going to have to recomputee all of that which is very math intensive

[00:15:18] which takes time. U but you can have the KV cache on VRM. It's just it's heavy and VRM is very expensive. So there are other solutions like LMC cache. LMC cache. So LM cache takes that KV cache which is very valuable because it's all of these like histories of your conversations um and all the math that was derived through that conversation. Uh it splits it into small chunks and it stores it either on RAM but that doesn't persist. So RAM is like DDR4, DDR5 um any like DDR3. Uh then you have also NVMe, SSD. So you can store the cache on different parts of your computer. The slower the memory you store it on, the longer it takes to verify if that cache exists or not when it is uh running. So if you are sure you're going to hit the cache, like if you have a hardness, like you're using cloud code, it starts with 30 or 40,000 tokens already in the cache. And those 30 or 40,000 tokens are hot, so they're always loaded in memory. You can put those on NVMe and fetching the cache when you send a prompt is going to be

[00:16:22] pretty fast because it knows it's there. It knows where to get it. Um, PI is very good at protecting the cache. Uh, it has one of the highest cache hits uh, cash hit rates uh, among all the different CLI tools. So, I recommend it if you're trying to save on tokens. You can plug in your codec subscription. Um, it's free and open source and uh, it's the basis of the app that you're looking at. So this is my app for running local models like local studio or lo what is it called uh lm studio and I so when I say hi or whatever this is going to pi so I'm running a pi process associated with this session um I personally have been using my own app and then codeex cla um do I use anything else zcode as well I like the desktop apps they take up more RAM but they make it easier to scale like my usage because I can see all the sessions flatten out. I I know there's stuff like couer, but it just takes time to adopt things. So, right now I'm using the apps a lot.

[00:17:24] Uh okay, let me see what's next. So, we talked about providers, OpenAI, Anthropic, uh whatever. Uh we talked about tools, how you format a tool, uh how you do the tool call. We talked about the agent loop. uh we talked about the translation layer which is uh the /ai repo in pi and that covers most of it. There's coding agent which this is what adds the tui. So the tui is the terminal user interface. Uh it also adds like a few more things. So extensions so that you can extend the model's capabilities without modifying the core code that is very clean and simple. And yeah I mean I think that covers most of pi. I will open it up to questions or any kind of Yeah, if you have any questions, let me know. I'll just leave quiet.

[00:18:16] >> How does it handle like sub agents and if you want to maybe use a local model and delegate to like a cloud model? >> So, because this is a terminal process, it has uh like the TUI the terminal user interface. There's also an SDK for PI. So, if you were trying to do anything complicated, uh you can use the SDK. Um, also all of the conversation, the session is just files. Like the history of the chat is JSON files or JSON L files. So you can actually fetch all of that out. Um, but to answer your question, because it's a terminal app, it the model has access to bash, it can use t-mi either a headless or a headed version of pi. So it could just like spawn itself again. And because there's all these models from all these providers that we saw how like the providers work and the models work and the login uh the model can call its like harness. So it can say pi run uh let's say uh opus for five.

[00:19:11] I'm just throwing words out there. Uh but that will run pi in the terminal that the original pi has access to and it'll be emitting a stream and that original pi can read what it's saying. Uh that's a little bit complicated to think about but just imagine like it can do what you're doing. It can prompt itself. Uh and then that next prompt is its own thread and its own history and its own thing. Any other questions? >> Um hey, sorry I joined a bit late, so I probably missed a couple pieces there. >> Hello friend. >> Hey Z, nice to talk to you again. curious if you did any comparatives like I'm curious as to why everybody's so excited about Pi and uh if like you know you can give a high level uh differentiation to something like you know using codecs or I don't know other harnesses.

[00:20:02] >> Yes. So there's a few reasons to use pi uh over other things. One is if you're a builder, if you like to build stuff, pi is such a great building block. It's uh very small lines of code like uh I think the whole thing is maybe 50,000 lines of code. The core is like a few thousand lines of code. U most repos these days are in the hundreds of thousands if not millions. So that helps a lot. Let's see where uh deep suite terminal bench where's pi agent um I don't think it's showing all the agents pi agent benchmark. So it's a great building block. It's very uh lightweight. It works great. Um the let's see PI agent. So I'm gonna have to find some benchmarks and leave them in the server. Uh I've seen a lot of them. Also from my experience when I've ran uh let's search my Twitter pi.

[00:20:54] Uh it has the highest cache hit rate which means it's the cheapest to to use with the same models like you. Uh so this is an example median time per task between pi and everything else. So pi doesn't have a large system prompt. It system prompt is 400 tokens. uh claw code system prompt is 20,000 tokens which is whatever you know now that models have a million tokens sure but there's a smart zone as well so the smart zone is the more tokens are in the context window the more unrelated stuff that the model is doing math on so if you're talking about coding and then some reason you start talking about I don't know some research paper now the next probable tokens are going to be tainted with this external information uh that is irrelevant so the the system prompt is very often irrelevant and it adds a lot of latency. So between the fastest and the slow like the so cloud code has the slowest hardness because it has the largest uh which I think it's actually 34,000 token system prompts before the tools and all this other stuff for cloud code. Um so let's say you do 12 prompts that alone is a million tokens that is being processed uh just in system prompts like you hit

[00:21:56] the cache and all that but so it it it has less stuff in it. It's a great building block. And then it has a pretty smart system where you can tell the agent like I want to add this feature to Pi. The agent then immediately has access to the PI docs. So it can search a specific way of doing something in Pi. It can pull that file and then it can hot reload. So let's say it adds a new file in the PI repo that uh shows you how many tokens or how many dollars you spent over the last day. So that's that's a plugin and an extension to the to the main core process. But the core process can just restart itself. So it can add new stuff in it and then restart itself. So it's cheaper, it's faster, there's less stuff going on. Uh it's better for local models because there's less tokens in the context window. Um and the accuracy on it is one of the highest on terminal bench. Uh I think it's like the second or third highest and obviously it's open source and the guy is like very the guy who made it is very nice. So the pink is pi. So I use that a lot. But the cash hits so the cash hits are like the the barred out zone. Uh so that's the cheapest possible tokens you can get. So from the same

[00:22:58] like like even if I'm using codeex those cash hits are much much cheaper 10 times cheaper than doing regular prefill the normal amount. So you can see that it has the highest cash hit rate by a lot. All right. So oh my PI is similar. Let's do OM update. It it is pi under the hood but it gives it all these extra tools and all these extra functionalities and features. So it's it's like a a good view of the future. Uh what this harness does well, it's pretty cool. Um I don't know why I have to restart it, but it's pulling all my PI stuff. There you go. Model. So what this thing does well is there are uh there are different roles for models. So there's a vision role. A vision is uh imagine you use GP 5.6 Luna for vision. You're getting it off your codec sub for nearly free. And then you have a local model like Deepseek V4 flash that doesn't have vision. Uh so you set Deepse V4 Flash as the main model. And um the MCP4 flashes the main model. The vision model would be Quen or what whatever it is that you want to use. Um you have the idea of an advisor.

[00:23:59] So the advisor reads all of the tokens that are being streamed to the model that you're using and it has your original prompt that just interjects the main thread if the main thread starts going off track or doing something irrelevant. So it stops like you can set a local model as the main model. You can set 5.6 six codecs as the adviser and then the main model is doing its thing and if it starts veering off or speaking in Chinese or whatever the main model sends it a message a prompt and tells it stop doing that you have to actually do this instead um or if the the main model is like done it just decides it's done and it actually isn't done with what you asked it to do the adviser can just kick it back into gear and tell it you need to do xyz next all the features so there's one request that is live it is in prefill so that means it's in prompt processing time and you can see here it's like reading about OM by looking at the package. It sees the session. It sees like what the providers were using, the models. It's just reading the stuff and it reads its own memory. There's like a memory system.

[00:24:58] You don't have to use this, but this is the point I'm trying to make is this is all PI under the hood. So you can have a super stripped down like Vim type experience or you can have this rocket launcher bazooka burn as much tokens as possible to get to a solution uh experience and you know it's going to be the cheapest you can get it because of the high cash hit rates the the optimal performance and all that and vice versa your local model could do great at being like an assistant and telling you what you need to do and all that um and then spawning frontier models or you can escalate. >> I actually have a separate question if you can hear me. >> Uh shoot. Yeah, please. >> Um, so what is your actual sort of perfect long form path setup if you if you want to deploy you know frontier model as an orchestrator but then also involve your local local to do some of the work. How do you make sure that sort of a long run task is done? I'm guessing it's a goal but then I'm assuming you also have some validation loops in there of what's the perfect structure that you found?

[00:25:58] >> Yeah, the what I do is so there's a platform called Zcode. So I have Zcode and it is a like similar to codeex but you can bring in your own providers. So if we go to uh let's see where's the model settings you can bring your own providers. You can just keep adding more and more providers. So you can set your local models up. You can connect your uh let's say we do responses and you can set up your your subscription to OpenAI and all that. And then you can set goals in this app. A goal is an automation. So if we go to automations, we see these are goals. So every x period of time, if the model isn't doing something already, uh the automation kicks in and sends it a prompt and tells it you need to keep doing whatever it is I prompted you to do at the start. Then um I'm sorry, I'm just trying to think.

[00:26:47] Uh yeah, so then I tell it to use T-Max. So T-Mox is a terminal multiplexer thing that lets sessions stay alive in the background. So T-mug it can just spawn another like some codecs can spawn a bunch of PI agents that use my local models or vice versa and I just have them do hill climbing. So hill climbing is where you structure the task in a way where there's some verifiable like reward isn't the right word but like there's a verifiable metric at each turn end and then that metric acts as the start off for the next the next turn. So, uh, let's say a hill climbing task is make this local model run faster. I don't really care how you do it. Just make it run faster. Um, and don't stop until I come and stop the loop. And you set that up and then it has an automation that runs every 30 minutes. It tries to make some change.

[00:27:34] Uh, it tries to install some kernel or like write its own kernels or whatever it does. And then it runs again. And if it hits a higher score, it updates a GitHub repo that then other people are using. Uh, if that makes sense. So, I structure most of the coding work now as these neverending cycles. You could do the same with like app development by saying use the app. So here's like a catch all prompt. Look at all the code in this app. Figure out what everything is doing. Write down a list of features that we support. Use all these features with browser use, computer use, then note down if there's any bugs or any unexpected outcomes under these specific features that we've outlined and then fix those features. Then run the thing again. Right? It it burns tokens like crazy, but it does make a difference. How to evaluate harnesses for our own tasks through different harnesses. Uh this is a great piece of advice that I heard from Theo. So whenever you are doing work and you have a task that was really hard to get done like where you struggled where uh Claude was like going in circles or Codex was going in circles save that session somewhere because that is going to be a benchmark for your local models for your whatever new frontier models that you're using. Um

[00:28:37] it's going to show you if capabilities are improving and if the workloads that you have that are difficult can be replicated with local models. Does that answer your question? Feel free to ask one one more question, guys. >> Last one. [laughter] >> I see someone piping. Once they finish, we'll answer that and we'll head out. What you should do after this, if you want to keep learning, is to read the code. So, I'm going to mute this because it's a little loud, but um read the code.

[00:29:35] That's pretty much it. You you'll get a lot of understanding of what's going on. You can have the model support you in reading the code, but try to read the code. Um, it's going to help you learn a lot faster than send you the repo. All right, I think that covers it. Thank you guys for coming. >> Thank you so much. Appreciate it. >> Thank you. and have a good rest of your
