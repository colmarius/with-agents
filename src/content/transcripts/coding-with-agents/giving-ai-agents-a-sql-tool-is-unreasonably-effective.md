---
title: "Giving AI agents a SQL tool is unreasonably effective"
summarySlug: "coding-with-agents/giving-ai-agents-a-sql-tool-is-unreasonably-effective"
sourceUrl: "https://www.youtube.com/watch?v=uMudSbZ79-A"
videoId: "uMudSbZ79-A"
capturedAt: "2026-09-05T09:59:14.977Z"
channel: "The SF Database Meetup"
language: "en"
kind: "auto-generated"
durationSeconds: 1097
---

## Transcript

[00:00:00] Uh, hi everyone. I'm Lewis. Uh, thanks for coming. Uh, so I've been through a few different rounds of basically just handing agents SQL and seeing what happens and then figuring out how to deal with all of the like potential downsides and consequences and risks and concerns. Um, and seeing some of the power of what happens when you do that and and it's actually been fun because a lot of companies have started to basically like build products that are just like productized versions of this. Um, you know, you've seen like Post Hog as like their agent just like querying things and a lot of other observability tools and a lot of other just tools that are like giving an MCP server that's really just like searching whatever data they have. Um, so I I keep hearing from people they're like, "Oh, you can't just like give it SQL because what if like XYZ?" And basically I've run into like almost every form of like what if and figured out like what you can do about it and kind of like how you can manage those risks and concerns and then like get a lot of really useful stuff out of it. So, uh, this all started with with a quip that I had a couple months ago. Um, everyone likes to call things like the

[00:01:02] unreasonable effectiveness of like whatever. Um, and mine is the unreasonable effectiveness of just putting everything in a database and then handing your agents a SQL tool. And then the like bit to add to that is how to do it safely and how to get the most out of it. Uh, so I work on conductor. Uh, it's this cloud agent thing. I think we have a few conductor people in the room somewhere. Um, and before that I worked at YC. We gave agents SQL inside the YCDB. We give agents SQL inside conductor on our cloud side of things. Um, and have learned kind of a few ways to do that effectively. So, it all started from this tweet a couple months ago. Um, that's me by the way. And it turned out that people liked this idea. Uh, and then, uh, one of my friends replied with this with this great Midway meme that was like, you know, everyone's like, "Oh, I need all my MCP servers and I need to have like rag search and I need to have like markdown and like browsers and all these things." And like actually like there's this really cool tool idea where like what if you just give your agents like one tool and like that one tool does everything. And I'm saying that basically like one tool you can give your agents that can end up doing pretty much everything is just

[00:02:05] like a SQL query. Um, and honestly though, that started a lot earlier, like 2025, early 2025, when like agents were like starting to be like kind of okay, but like not really all that good yet. Um, and at YC, uh, Jared, one of the partners, basically was just like, [ __ ] it, let's just give like the agent like a SQL tool that can like query production and like see what happens and like if like that ends up being a problem, like we'll deal with it. Um, and they recently came out with a with a Lite Cone uh video. So, so if you're interested in this, um, there's a good Litecoin episode on YouTube where they they get into a lot of the stuff that I worked on while I was working there. Um, where we basically just like handed agent SQL on YC's database which has like 20 years worth of like startup stuff. Uh, and like a lot of like really interesting stuff just like fell out from doing that. Um, and so that part helped inspire the talk. Uh, but of course like Paul Graham was like that made the hair on my arm stand up. This is horrifying. Like how did you possibly make this safe? And so some of this is just like how do we make it safe? Um and then at Conductor recently uh like since that tweet uh we built something inspired by this idea for our API for searching conversation transcripts um because basically what we found was that

[00:03:08] conductor being a local Mac app we had a SQLite database that would have all your conversation transcripts. And so we would very frequently metaprompt um in the local story. We were like okay find me the conversation where I was doing like XYZ uh by like querying my SQLite database that has like all that stuff. And it was really powerful having all that stuff in SQLite able to be queried by our local agents. And then we went and started building conductor cloud and we were like damn we can't do this anymore with conductor cloud because like it's not all in SQLite anymore it's in like postgress on the server side. So like how do we make the same thing possible that we used to have where we can just like metarrompt query through like all the stuff we have. Um and so building that I realized like oh maybe there's like a fun conference talk to make out of this idea and then Christian sitting over there replied and said you should come do that at one of our meetups. So now here we're doing that. And now I'm here to convince you that like you can really just give the agent SQL and it'll be fine. Trust me. Uh and here's how. So uh by the way, as uh Kristen was just given the pitch about planet scale, uh we use planet scale at conductor. Um and he was talking about how easy it is to give your database

[00:04:11] more storage. And actually one thing that uh I need to do this week is give our database more storage because we're getting kind of full. So, I thought I would just do it live just to like show how easy it is. You can just click this like view upgrade options thing and then you can just be like, "Wow, I want Where's Where's the one we've got?" And then just like click the button and like that's it. Now, like we're done. We just made our database bigger. Live demo. Oh, did I did I hit apply? I thought I hit apply. Oh, apply changes. Oh, yeah. Okay, let's do it. We're doing it. Anyway, um back to giving agents SQL. Um so, uh as I mentioned before, uh the conductor API. So, we run cloud agents.

[00:05:06] Uh you like similar to like if you're running like local cloud code or codeex or whatever, you can do that all in our same API that you can do it locally, but you can also do it in cloud where we run it in sandboxes and we have a bunch of like database and multiplayer stuff going on. Um, and to enable that meta prompting that we had against our local SQL light, basically we built an API that among other things has like literally a SQL endpoint where it's like send us some SQL and we're going to run it. Uh, and of course people are like no, oh my god, you can't do that. Like that's so dangerous. Like everyone knows like you can't just like let people like send you SQL that you run. Um but like actually like thinking about it like zooming back like many years ago uh I was like working on a platform team supporting a data science team where we wanted to let them run arbitrary SQL and like in a way like there's kind of like a trust boundary there and you have to figure out like how do you make it okay for your data science team to run arbitrary SQL. So like you know one answer is like I don't know you like do a nightly dump and then you like put it somewhere else but like that's a bunch of work and nobody really wants to do that. So like what did you actually do?

[00:06:02] you pointed metabase at a read replica and you like turn down all the timeouts and you just like tell your data science team like here's metabase like do whatever you want it's a read replica it's a read replica like you probably can't hurt anything uh right like like how many other people have like done something like that or like worked somewhere the like something like that going on I see a few hands okay I'm not the only one that's done this and it turns out that basically you can just like do the same thing with agents um you can just be like I'm going to pretend like agents are kind of like my data science team and I need to put some guards in place but like if we do it and we make it safe like it's going to be fine Um so a few of those things first obviously read replicas planet scale makes those easy readon roles uh lots of limits your statement timeout your connect timeout um you know lots of uh lots of just like making sure that you're not going to suddenly send like you know 10 megabytes of data over the wire. So you can do like row count limit, you can do query uh result set limit. There's all kinds of things that you can like turn down. Um and then like audit logs to just like know what's going on. Um, and there's also this really neat trick that we came up with for the conductor API in particular

[00:07:05] where we do this limited view scope thing. And so I had an agent go remind me like how exactly does that thing work? Um, and basically we do this whole like local config thing where we um, we can scope a table down to just like the tenant that is querying um, by using Postgress config and then like set a bunch of like local stuff and then like run it all inside of an immediately rolled back transaction. Um, and by doing that, uh, plus like a view over a table, we basically can restrict what the agent is actually able to query and to where it's not really giving the agent arbitrary SQL. It's just that the agent thinks it has arbitrary SQL and like actually you've like limited it to like whatever you want to limit it to. Um, so there's like all kinds of like cool tricks like that that you can do. Um, and then like now that we've loosely convinced ourselves that like it's possible to do this safely, let's talk about like why does it work so well? And the key idea here is that agents don't work like people work. Um, you can just feed it your schema and like it's going to know your schema like better than like any person ever would. Um, you can like pay as much attention to your

[00:08:07] schema as you think as you as you want, but like it's just going to know your schema better than you will. Um, and in fact, if you use some kind of like uh like model based uh framework um where like your model code files um have like additional context about like what means what like the semantic intent of all your columns just like feed those to it too. Um and like it just is going to like know how stuff works. And there's all these things that like when humans are writing SQL queries they would like never do that like agents will just like run in circles and do and they'll like make it work. Like trying a wear clause where you just have like 12 different keywords strung together. um or like reading a hundred rows of stuff or like chasing foreign keys across tables. Like humans just like can't do these things or don't have the patience or like the grit to like do these things. Um and so you can just like expect agents to kind of just like brute force their way through things um in a way that like makes SQL actually work in ways that it doesn't really work for humans. Um like they they will just go and do it. Uh they're better at brute force. They'll read more than humans would read.

[00:09:03] They'll read things that humans wouldn't bother to read. Um there was this recent uh Jeff Huber tweet. Um it's literally just that like AI is better at brute forcing than we are. And so this is just like one case where we can stick them to brute forcing things and get good outcomes. Uh and then taking this back another framing remember like in 2023 and 2024 like everyone was all really excited about doing like vector embedding like code search indexing stuff and then like cloud code came along and was like [ __ ] it. What if we just like do GP instead? And that turned out that that was like a huge like shift. And like basically SQL versus like vector search is like kind of sort of the same thing uh once you set it up right because then like one tool beats 20. You already have code mode. Like SQL is kind of code. Like like we didn't even like talk about code mode when we started handing SQL to agents, but like in hindsight it's kind of like code mode. Um and like actually at one point when we were doing SQL over the YC database, we also had like a vector search thing. Um, but we kept finding that the agent would like reach for the vector search when it didn't actually

[00:10:05] get the results we wanted it to get and that like deleting the vector search tool and having it just like write SQL queries like actually works better. Um, and okay, tips and tricks, random tips and tricks. Uh, there's this one neat trick you can do with your SQL tool. So, you're going to like make a SQL tool and give it to your agents. Um, just make the SQL return commaepparated values instead of JSON. and then like suddenly your result set like takes half as many tokens. So if you're doing this CSV instead of JSON, um the other key idea to a lot of this is that once you've like done the initial like let's put stuff in a database and let's give agents access to query the database is that now like it actually motivates you to think about your schema more which also makes things nicer for humans and makes things nicer for your codebase.

[00:10:55] But really, it's just that like schema design is kind of your tool design and your prompt engineering and your context assembly like allin-one. Um, where if you just make your schema better, then like your agents just get better. Um, and the the other idea to kind of like iterate over time once you get that initial thing going is you can start defining views to focus the agent where you'll be able to like metarrompt all the queries that it runs and go, okay, like it keeps having to join these three tables and select like these 12 columns. Um, and it keeps having to like think about the schema and do a few like trial and error iterations to get there. So like let's just like make a view and like add to the system prompts to like use this view and it basically just short circuits it where now instead of having to do like 10 or 20 tool calls to figure things out, it just like gets there in one tool call. Um, it's kind of like creating a skill but it's like a SQL native version of a skill.

[00:11:47] Um, oh, this this was the idea that should have been the prerog for that. But when there are errors, like you just you just give them back to the agent and they iterate and they self-correct. Um, and basically when you see that it's hitting a bunch of errors, like that kind of like tells you where either you're missing context about the schema or you're missing context about what the like semantic meaning of a column is or like you should just like define a view or something like that. Um, and then one of the last key ideas here is like widening our horizons of what everything means when we say like let's put everything in a database. Um, so you might at first think like okay all of like the like business data or like you know all of the like stuff that is like our users like application entity state. Um, but you can also just be like, "Okay, any third party that like used to be a thing we would just like hit an API to go get, like let's just like keep a mirror of that in our database instead."

[00:12:38] So like a lot of people who or a lot of companies that are like an API now, like Slack for example, um, they have rate limits because they want you to like pay them a bunch of money to like use their MCP and like their agent and all this stuff. Everyone's like trying to like lock down their data. Um, but you can just like set up persistent things that are quietly taking every Slack message that ever hits your workspace and just like putting it in your database. And now you can just like query that from your database instead of having to like go hit the Slack API. Um, and then you just like never get really limited by Slack again. Like there's this whole idea like, oh, you need to like own your context. Like everyone's really excited about like owning their context instead of like letting someone else own their context. Um, and basically the way you do that is you just like take all of it and you put it in a database. Um, and then the other thing you can do if you don't want to go and like put it in a database is you just like make it look like a database. So you can like point duct DB at things like ductb basically knows how to read anything anywhere these days and you can just make agents think that they're talking to SQL by like putting duct DB in between it. Um, or you can like take it from an MCP server or an API or wherever else it

[00:13:41] otherwise lives and just like write a little thing that makes it be in your database instead. Um, and then you don't have to worry about rate limits or about adding other tools to your agent for them to know how to go like hit this API or use this CLI or use this MCP or any of that. Um, and then from there, once you're once you've got this loop going where you're running a bunch of agent queries, you can improve it over time by audit logging all the queries, seeing where the agent is hitting errors, and then just like metaparprompting where it misses and then figuring out like what views to add and what like known good, common, useful queries to add to the system prompt. And once you get that loop going, um, you can just give the agent SQL and it'll be fine. Trust me. Um, yeah, that's it. Any questions? Um, basically whatever views you define.

[00:14:39] So, so you have a role that is allowed to uh to read only whatever you tell it it's able to read. And then um only if you've like actively created a view that is able to view that stuff will the agent be able to query it. And so then um just don't put PII in those views, I guess. I haven't figured that out yet. I mean, you know, that's that's more like ultra yellow mode, I guess. I mean like file systems are cool. Everyone likes file systems. Um, I'm not convinced that a whole bunch of files with data in them is the thing that agents are best at querying because they know how to write SQL as a pattern of how to query things and they know how to use SQL to like join and slice and

[00:15:41] iterate things. Um, yeah, a lot of stuff is presenting as a file system these days. And there is a totally like a whole different story of like let's present as a file system and then have like like there's like this idea of like the one magic tool is Bash and it knows how to talk to a file system and then there's like this other idea like the one magic tool is SQL. Um, they're both really interesting. I I don't know if I like have a strong opinion about one versus the other, but I think um I think more the idea is like there's a lot of reasons why people think they can't give an agent sequel and like here's here's like what happens if you just like do do that. Um I suppose so. Yeah, I mean at one point there was there was a query that um I had an agent help make sense of that was a composition of a whole bunch of different views that nobody could really grock. Um that kind of happens with our codebase too though. So like I don't know if that's really that concerning but um

[00:16:46] the from from the operational perspective though it's just like you have timeouts and limits on what it's allowed to do and as long as the queries within that like I don't know like do we really need to understand exactly what the query is doing if it gets the outcome we want. Last one. Yeah, that that's where it can be tricky is actually um like sometimes your schema is like actually bigger than a context window. Um so like hopefully your schema is not bigger than a context window and if it is bigger than a context window then what you want to do is you want to start defining your like curated views of what the agent should look like and then like choose like which tables and views you give to it as that initial context window. Um otherwise like obviously you're just going to be um out of luck. The the other thing is basically that as it's querying, running around, trying to figure out like what it's doing, you need to make sure that the result sets it's pulling are not any bigger than they have to be because it when it starts like iterating through big results set, it becomes really easy to blow a context window. Um, so yeah, that's definitely something to pay

[00:17:47] attention to, but I think um for the most part like you know the worst thing that happens is you decide that like you have to have one agent that knows how to query these tables and a different agent that knows how to query those tables and you give each one like a different initial context set. Um, but I would say in general like I don't know like a 100k tokens worth of knowing about the database and knowing about like the semantics of like what all your code knows about those columns and how they're used um is a decent starting point.
